function booWho(bool) {
  // What is the new fad diet for ghost developers? The Boolean.
  // lol
  var result;
  // check if input if a boolean value, return true if so, false if not so
  ((bool===true) ||(bool===false)) ? result = true : result = false;
  return result;
}

booWho(null);

